# Auto-generated config file for voice ${voice.name}

name = ${voice.name}
locale = ${voice.language}

####################################################################
####################### Module settings  ###########################
####################################################################
# For keys ending in ".list", values will be appended across config files,
# so that .list keys can occur in several config files.
# For all other keys, values will be copied to the global config, so
# keys should be unique across config files.

hmm.voices.list = ${voice.name}

# If this setting is not present, a default value of 0 is assumed.
voice.${voice.name}.wants.to.be.default = 0

# Set your voice specifications
voice.${voice.name}.gender = ${voice.gender}
voice.${voice.name}.locale = ${voice.language.equalsIgnoreCase(voice.region) ? voice.language : voice.locale}
voice.${voice.name}.domain = general
voice.${voice.name}.samplingRate = ${voice.samplingRate}

# HMM Voice-specific parameters 
# parameters used during models training 
# MGC: stage=gamma=0 alpha=0.42 linear gain (default) 
# LSP: gamma>0  
#          LSP: gamma=1 alpha=0.0  linear gain/log gain 
#      Mel-LSP: gamma=1 alpha=0.42 log gain 
#      MGC-LSP: gamma=3 alpha=0.42 log gain 
voice.${voice.name}.alpha = ${voiceAlpha}
voice.${voice.name}.gamma = ${voiceGamma}
voice.${voice.name}.logGain = false

# Parameter beta for postfiltering 
voice.${voice.name}.beta = ${voiceBeta}

# HMM Voice-specific files
# Information about trees
voice.${voice.name}.Ftd = jar:/marytts/voice/${voice.nameCamelCase}/tree-dur.inf
voice.${voice.name}.Ftf = jar:/marytts/voice/${voice.nameCamelCase}/tree-lf0.inf
voice.${voice.name}.Ftm = jar:/marytts/voice/${voice.nameCamelCase}/tree-mgc.inf
voice.${voice.name}.Fts = jar:/marytts/voice/${voice.nameCamelCase}/tree-str.inf

# Information about means and variances PDFs 
voice.${voice.name}.Fmd = jar:/marytts/voice/${voice.nameCamelCase}/dur.pdf
voice.${voice.name}.Fmf = jar:/marytts/voice/${voice.nameCamelCase}/lf0.pdf
voice.${voice.name}.Fmm = jar:/marytts/voice/${voice.nameCamelCase}/mgc.pdf
voice.${voice.name}.Fms = jar:/marytts/voice/${voice.nameCamelCase}/str.pdf

# Information about Global Mean and Variance PDFs
voice.${voice.name}.useGV = true
voice.${voice.name}.maxMgcGvIter = ${voiceMaxMgcGvIter}
voice.${voice.name}.maxLf0GvIter = ${voiceMaxLf0GvIter}
voice.${voice.name}.Fgvf = jar:/marytts/voice/${voice.nameCamelCase}/gv-lf0-littend.pdf
voice.${voice.name}.Fgvm = jar:/marytts/voice/${voice.nameCamelCase}/gv-mgc-littend.pdf
voice.${voice.name}.Fgvs = jar:/marytts/voice/${voice.nameCamelCase}/gv-str-littend.pdf

# A context features file example for start-up testing.
voice.${voice.name}.FeaFile = jar:/marytts/voice/${voice.nameCamelCase}/${voiceFeatureFile}.pfeats

# Tricky phones file in case there were problematic phones during training, empty otherwise.
voice.${voice.name}.trickyPhonesFile = jar:/marytts/voice/${voice.nameCamelCase}/trickyPhones.txt

# Information about Mixed Excitation
voice.${voice.name}.useMixExc = true

# Filters for mixed excitation 
# File format: one column with all the taps, where the number of taps per filter = numTaps/numFilters 
voice.${voice.name}.Fif = jar:/marytts/voice/${voice.nameCamelCase}/mix_excitation_filters.txt
# Number of filters in bandpass bank
voice.${voice.name}.in = 5

# Information about acousticModels (if true allows prosody modification specified in MARYXML input)
voice.${voice.name}.useAcousticModels = true

# acoustic models to use (HMM models or carts from other voices can be specified)
# (uncoment to allow prosody modification specified in MARYXML input)
voice.${voice.name}.acousticModels = duration F0

voice.${voice.name}.duration.model = hmm
voice.${voice.name}.duration.attribute = d

voice.${voice.name}.F0.model = hmm
voice.${voice.name}.F0.attribute = f0
